# Cross-cutting integration & soak fixtures (M1+)

M1 places here: a fixture script JSON plus recorded offline LLM responses so the full pipeline
runs end-to-end regression-style without network or API spend. Fault-injection harness and soak
driver arrive in M5 per PROJECT_PLAN.md §8.
